import "package:flutter/material.dart";

class AppColors {
  //Main Colors
  static const Color primary = Color(0xFF4CD137); // hijau utama (logo)
  static const Color primaryLight = Color(0xFF6EEB5A); // versi lebih terang
  static const Color primaryDark = Color(0xFF2FA01C); // versi lebih gelap
  static const Color accent = Color(0xFF00C853); // aksen hijau fresh

  //Background Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  //Error Colors
  static const Color error = Colors.red;

  //Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  //Line (Divider and Border) Colors
  static const Color divider = Color(0xFFE0E0E0);
  static const Color border = Color(0xFFE0E0E0);
}
